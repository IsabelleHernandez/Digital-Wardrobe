{
  "extends": "expo/tsconfig.base",
  "compilerOptions": {
    "strict": true,
    "paths": {
      "@/*": [
        "./*"
      ]
    },
    "types": ["nativewind/types"]   // <-- Added for NativeWind className support
  },
  "include": [
    "**/*.ts",
    "**/*.tsx"
  ]
}

